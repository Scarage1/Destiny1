# Required for LLM-powered (non-template) query generation and synthesis
GEMINI_API_KEY=

# Optional fallback provider key (used when Gemini is unavailable/errors)
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-8b-instant

API_PORT=8000
WEB_PORT=3000
